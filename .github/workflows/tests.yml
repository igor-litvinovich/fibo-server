name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Setup Node
      uses: actions/setup-node@v1
      with:
         node-version: '10.x'
    - name: Run Unit-tests
      run: |
        npm i
        npm test
